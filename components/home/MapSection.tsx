import Map from './Map';

const MapSection = () => {
  return (
    <>
      <Map
      // onLoad={onLoadMap}
      // initialZoom={initialZoom}
      // initialCenter={initialCenter}
      />
    </>
  );
};

export default MapSection;
